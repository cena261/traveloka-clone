package com.cena.traveloka.iam.enums;

/**
 * Entity type enum for sync events
 * Based on application entities
 */
public enum EntityType {
    USER,
    ROLE,
    PROFILE,
    PREFERENCE,
    SESSION
}